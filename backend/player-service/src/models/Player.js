// Correct structure at the top of the file
import mongoose from 'mongoose';

const playerSchema = new mongoose.Schema({ // <-- The opening brace { must be present
  // ... your field definitions go here inside this object
:cite[4]);
